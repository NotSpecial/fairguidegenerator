\documentclass{amivbooklet}

% Start on even page for preview
\usepackage[2-]{pagesel}

% Add text indicating preview to footer
% \fancyfoot[EL]{Vorschau Links / Preview Left}
% \fancyfoot[OL]{Vorschau Rechts / Preview Right}

\begin{document}
% Placeholder for first (odd) page, which wil be skipped
\hbox{}\newpage

((* for company in companies *))
\name{((( company.name|t )))}
\website{((( company.website|t )))}
\booth{((( company.booth|t )))}
\logo{((( company.logo )))}
\boxes{%
    ((* if 'ITET' in company.interested_in *))\itet((* endif *))
    ((* if 'MAVT' in company.interested_in *))\mavt((* endif *))
    ((* if 'MTEC' in company.interested_in *))\mtec((* endif *))
}
\contact{((( company.contact|t )))}
\employees{((( company.employees|t )))}
\offers{((( "\n".join(company.offering)|t )))}
\about{((( company.about|t )))}
\focus{((( company.focus|t )))}

% Even and Odd preview
\companywithadleft{(((company.ad)))}
\companywithadright{(((company.ad)))}

((* endfor *))

\end{document}
