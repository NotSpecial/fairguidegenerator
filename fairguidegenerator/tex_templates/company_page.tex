\documentclass{amivbooklet}

\begin{document}

\name{((( name )))}
\website{((( website )))}
\booth{((( booth )))}
\logo{demo_logo.png}
\boxes{%
    ((* if 'ITET' in interested_in *))\itet((* endif *))
    ((* if 'MAVT' in interested_in *))\mavt((* endif *))
    ((* if 'MTEC' in interested_in *))\mtec((* endif *))
}
\contact{((( contact|newline )))}
\employees{%
    ((( employees_ch ))) in der Schweiz\\
    ((( employees_world ))) in der Welt%
}
\offers{((( "\\".join(offering) )))}
\about{((( about|l|newline )))}
\focus{((( focus|l|newline )))}

% Demo left side
\companypage
% Demo right side
\companypage
\end{document}
